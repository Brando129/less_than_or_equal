# Create a function that takes a number as its only argument and returns true if it's less than or equal to zero, otherwise return false.

def less_than_or_equal(num):
    if num <= 0:
        return True
    else:
        return False
    
print(less_than_or_equal(1))